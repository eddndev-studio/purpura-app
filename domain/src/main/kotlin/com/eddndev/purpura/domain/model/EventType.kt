package com.eddndev.purpura.domain.model

// Codigos ASCII verbatim del contrato (04-api-contract) y del modelo canonico
// (03-domain-entities). Las etiquetas de UI en espanol viven en recursos string.
enum class EventType {
    cita,
    junta,
    entrega_proyecto,
    examen,
    otros,
}
